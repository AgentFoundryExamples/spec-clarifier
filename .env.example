# ============================================================================
# Spec Clarifier Environment Configuration
# ============================================================================
# Copy this file to .env and customize for your environment

# ----------------------------------------------------------------------------
# LLM Provider Configuration
# ----------------------------------------------------------------------------
# LLM_PROVIDER: Choose which LLM provider to use
# Options: "dummy" | "openai" | "anthropic"
# Default: "dummy" (no API keys required, for testing/development)
#
# - "dummy": Returns mock responses, perfect for local development and CI/CD
# - "openai": Use OpenAI GPT models (requires OPENAI_API_KEY)
# - "anthropic": Use Anthropic Claude models (requires ANTHROPIC_API_KEY)
LLM_PROVIDER=dummy

# ----------------------------------------------------------------------------
# API Keys for Real LLM Providers
# ----------------------------------------------------------------------------
# Only required when using real providers (openai or anthropic)
# Leave blank or unset when using dummy provider

# OpenAI API Key
# Required when LLM_PROVIDER=openai
# Get your key from: https://platform.openai.com/api-keys
#OPENAI_API_KEY=sk-your-openai-api-key-here

# Anthropic API Key  
# Required when LLM_PROVIDER=anthropic
# Get your key from: https://console.anthropic.com/
#ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key-here

# ----------------------------------------------------------------------------
# Application Settings
# ----------------------------------------------------------------------------
# All application settings use APP_ prefix

# Application metadata (optional, defaults provided)
# APP_APP_NAME=Spec Clarifier
# APP_APP_VERSION=0.1.0
# APP_APP_DESCRIPTION=A service for clarifying specifications

# Debug mode (enables detailed error messages in responses)
# Default: false (recommended for production)
APP_DEBUG=false

# Show job results in GET /v1/clarifications/{job_id} responses
# Default: false (results hidden in production, use separate result endpoint)
APP_SHOW_JOB_RESULT=false

# Enable debug endpoint GET /v1/clarifications/{job_id}/debug
# Default: false (disabled for security)
APP_ENABLE_DEBUG_ENDPOINT=false

# Enable config admin endpoints (GET/PUT /v1/config/defaults)
# Default: true (can be disabled for security in production)
APP_ENABLE_CONFIG_ADMIN_ENDPOINTS=true

# ----------------------------------------------------------------------------
# LLM Default Configuration
# ----------------------------------------------------------------------------
# These defaults are used when no per-request config is provided
# Note: These are fallback values; LLM_PROVIDER takes precedence

# Default LLM provider (legacy setting, use LLM_PROVIDER instead)
APP_LLM_DEFAULT_PROVIDER=openai

# Default model for the provider
APP_LLM_DEFAULT_MODEL=gpt-5

# ----------------------------------------------------------------------------
# Allowed Models per Provider
# ----------------------------------------------------------------------------
# Comma-separated lists of allowed models for each provider
# Used for validation when creating/updating configs

# OpenAI allowed models
APP_ALLOWED_MODELS_OPENAI=gpt-5,gpt-5.1,gpt-4o

# Anthropic allowed models
APP_ALLOWED_MODELS_ANTHROPIC=claude-sonnet-4.5,claude-opus-4

# Dummy allowed models (for testing)
APP_ALLOWED_MODELS_DUMMY=test-model,dummy-model

# ----------------------------------------------------------------------------
# CORS Configuration
# ----------------------------------------------------------------------------
# Configure CORS for frontend applications

# Allowed origins (comma-separated)
APP_CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://127.0.0.1:3000,http://127.0.0.1:8000

# Allow credentials (cookies, authorization headers)
APP_CORS_ALLOW_CREDENTIALS=true

# Allowed HTTP methods (* for all)
APP_CORS_ALLOW_METHODS=*

# Allowed HTTP headers (* for all)
APP_CORS_ALLOW_HEADERS=*

# ----------------------------------------------------------------------------
# Quick Start Examples
# ----------------------------------------------------------------------------
#
# Example 1: Local Development (Dummy Mode - No API Keys Required)
# ----------------------------------------------------------------
# LLM_PROVIDER=dummy
# APP_DEBUG=true
# APP_SHOW_JOB_RESULT=true
# APP_ENABLE_DEBUG_ENDPOINT=true
#
# Example 2: Production with OpenAI
# ----------------------------------
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-your-real-api-key
# APP_DEBUG=false
# APP_SHOW_JOB_RESULT=false
# APP_ENABLE_DEBUG_ENDPOINT=false
# APP_ENABLE_CONFIG_ADMIN_ENDPOINTS=false
#
# Example 3: Production with Anthropic
# -------------------------------------
# LLM_PROVIDER=anthropic
# ANTHROPIC_API_KEY=sk-ant-your-real-api-key
# APP_DEBUG=false
# APP_SHOW_JOB_RESULT=false
# APP_ENABLE_DEBUG_ENDPOINT=false
# APP_ENABLE_CONFIG_ADMIN_ENDPOINTS=false
