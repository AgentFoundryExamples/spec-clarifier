# Implementation Notes/Issues

- Something is wrong with the config loading.
- App works and is printing where it would send to a downstream service(agent foundry execution pipeline)
- Needs a way to be run in cloud run(easy enough but env loading needs fixing.)
- Frontend needs to pass correct indices(easy enough again)